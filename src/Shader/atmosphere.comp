#version 460 core

#include "common.glsli"

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

float Noise(vec2 InP)
{
    vec3 P = fract(InP.xyx * 0.1031);
    P += dot(P, P.yzx + vec3(33.33));
    float Result = abs(fract((P.x + P.y) * P.Z));
    return(Result);
}

void main()
{
}